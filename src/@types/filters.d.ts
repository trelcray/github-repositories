export interface Itype {
    types: [{
        id: string;
        all: string;
        public: string;
        private: string;
    }
    ]
}

export interface Iorder {
    orders: [{
        id: string;
        name: string;
        updated: string;
    }
    ]
}